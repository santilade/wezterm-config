# wezterm-config

My personal [WezTerm](https://wezfurlong.org/wezterm/) configuration.

## Install

Clone into the WezTerm config location:

```sh
git clone https://github.com/santilade/wezterm-config.git ~/.config/wezterm
```

On Windows: clone to `%USERPROFILE%\.config\wezterm`.
